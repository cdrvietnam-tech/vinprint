# Review thay đổi VinPrint.vn

*Nhánh: `agent-evolution/restore-pricing-posters` · Ngày: 30/07/2026 · Trạng thái: chưa push*

---

## 1. Tổng quan đã làm

| Nhóm | Nội dung |
|---|---|
| Đo lường | Gắn **GA4 trực tiếp** (`G-0XQZ3FCJN0`), một đường đo duy nhất; GTM tắt để tránh đếm trùng. Sự kiện: `click_zalo`, `click_phone`, `view_pricing`, `open/load_google_map`. |
| Quản trị nội dung | Trang chủ đọc nội dung động; trang `/admin/noi-dung` sửa **9 khối** (Hero, số liệu, bảng giá, quy trình, đánh giá, FAQ, CTA, liên hệ, footer + menu). Lưu JSON trên R2, có mặc định dự phòng. |
| Trung tâm quản trị | `/admin` (cửa ngõ) + thanh điều hướng chung; `/admin/hinh-anh` (sửa ảnh/video **+ đổi tiêu đề**); `/admin/bai-viet` (bảng theo dõi bài blog, chỉ xem). |
| Trang chủ | Khối "Sản phẩm đang hot" đổi thành **lưới 2 hàng** (hover, nút xem thêm, không cắt ảnh). Sửa cắt ảnh ở nhiều khối (object-contain). Sửa phần đánh giá cho trung thực. |
| Trang sản phẩm | `/san-pham` thêm **lưới ảnh lớn — tiêu đề dưới**, click mở chi tiết. |
| An toàn dữ liệu | Worker validate cấu trúc nội dung khi **ghi (422)** và khi **đọc** (dữ liệu hỏng → dùng mặc định). |

---

## 2. File thay đổi

**Sửa (23 file):** `worker/index.ts`, `app/layout.tsx`, `app/lib/analytics.ts`, `app/lib/products.ts`, `app/san-pham/page.tsx`, `app/san-pham/[slug]/page.tsx`, và các khối trang chủ (`Hero, HotProductsMarquee, Pricing, Process, Reviews, FAQAndZaloCTA, MapSection, Header, Footer, MobileActionBar, Gallery, Categories`), `HeroImageAdmin`, `VideoAdmin`, `.env.example`, test marquee.

**Thêm mới:** `app/lib/site-content.ts`, `app/components/SiteContentProvider.tsx`, `app/components/GoogleAnalytics.tsx`, `app/components/admin/{AdminNav,ContentAdmin,BlogDashboard}.tsx`, `app/admin/{page,noi-dung,bai-viet}`, `HUONG-DAN-GA4.md`, 2 file test, 3 ảnh sản phẩm tách riêng.

**Xóa:** `HUONG-DAN-GA4-GTM.md` (thay bằng bản GA4).

---

## 3. Đánh giá theo hạng mục

| Hạng mục | Mức | Ghi chú |
|---|---|---|
| Đo lường | 🟢 Tốt | Một đường đo, có test chống trùng. Cần bật "key event" trong GA4. |
| Bảo mật ghi/đọc nội dung | 🟢 Tốt | Validate 2 chiều; dữ liệu hỏng không phá trang. |
| Quản trị (UX) | 🟢 Tốt | Gộp một trung tâm; sửa được chữ, số, ảnh, tiêu đề ảnh. |
| Trang chủ / trang sản phẩm | 🟢 Tốt | Ảnh không cắt; lưới rõ ràng; đánh giá trung thực. |
| SEO nội dung động | 🟡 Chấp nhận | Chữ mặc định có trong HTML; chữ đã sửa nạp bằng JS (Google chạy JS vẫn index). Không chặn push. |
| CMS sản phẩm / soạn bài viết | ⚪ Chưa làm | Quyết định phạm vi — xem mục 6. |

---

## 4. Trạng thái các điểm review trước

**Standards**
1. 🟢 Đọc R2 chưa validate → **đã sửa** (`readValidatedSiteContent`, trả `{}` nếu hỏng) + có test.
2. 🟢 Hướng dẫn GA/GTM lỗi thời → **đã thay** bằng `HUONG-DAN-GA4.md`, xóa file GTM.
3. 🟢 Thiếu test bảo vệ → **thêm 13 test** (422/400, fallback đọc hỏng, `response.ok`, GA4 một đường, MobileActionBar).
4. 🟡 Nội dung nạp phía client → đánh đổi có chủ đích, **không chặn push**.

**Spec**
1. 🟢 Thanh mobile dùng số cố định → **đã sửa** (đọc từ quản trị) + test.
2. ⚪ Chưa có CMS sản phẩm; bài viết chỉ xem → **quyết định phạm vi**, chưa làm (xem mục 6).
3. 🟢 Ảnh dùng chung path → **đã tách** 3 sản phẩm sang ảnh riêng; `Categories` không render nên vô hại.

---

## 5. Kết quả kiểm thử (chạy thật trên bản Linux)

| Gate | Kết quả |
|---|---|
| `tsc --noEmit` | ✅ 0 lỗi |
| `vinext build` | ✅ Build complete |
| `content:audit` | ✅ 16 bài, đều ≥95 |
| `rendered-html.test.mjs` | ✅ 26/26 |
| `tests/*.test.ts` | ✅ 34/34 (gồm 13 test bảo vệ mới) |
| `eslint` | ⏳ Không chạy xong trong sandbox (config Next quá nặng) — quy tắc từng đỏ đã sạch; **chạy `npm run lint` trên máy để chốt** |

---

## 6. Việc còn lại / khuyến nghị

1. **CMS sản phẩm + soạn/sửa bài viết trên web** (Spec #2): tính năng lớn, nên làm đợt riêng có test. Chờ anh duyệt.
2. **Ảnh sản phẩm riêng biệt**: 3 ảnh vừa tách hiện là bản sao giống nhau — anh nên tải ảnh thật khác nhau cho `tem-bao-hanh`, `sticker-trang-tri`, `tem-phu-san-pham` qua `/admin/hinh-anh`.
3. **Rà số liệu marketing** (90.000+ / 211.000+ / 100K+ / 32k): thống nhất cho nhất quán — giờ sửa trực tiếp trong `/admin/noi-dung`.
4. **GA4**: sau deploy, vào Realtime kiểm tra và bật *key event* cho `click_zalo`, `click_phone`.

---

## 7. Đưa lên production

```
npx tsc --noEmit
npm run lint
npm test
git add -A
git commit -m "feat: quan tri noi dung/anh/bai viet, GA4, luoi san pham, validate + tests"
git push
```
Rồi gộp nhánh `agent-evolution/restore-pricing-posters` vào `main` (PR) để Cloudflare build và cập nhật vinprint.vn.
